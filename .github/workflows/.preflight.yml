name: Preflight

on: { workflow_call: {} }

jobs:
  publish-dry-run:
    runs-on: ubuntu-24.04
    steps:
      - uses: actions/checkout@v6
      - name: Run publish dry-run
        uses: soltiHQ/actions/taskfile@main
        with:
          command: ci/publish-dry-run
