name: Preflight

on: { workflow_call: {} }

jobs:
  publish-dry-run:
    runs-on: ${{ vars.RUNS_ON }}
    steps:
      - uses: actions/checkout@v6
      - name: Run publish dry-run
        uses: soltiHQ/actions/taskfile@main
        with:
          command: ci:cargo/publish-dry-run CRATE=taskvisor
