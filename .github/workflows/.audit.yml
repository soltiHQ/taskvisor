name: Audit

on: { workflow_call: {} }

jobs:
  audit:
    runs-on: ubuntu-24.04
    steps:
      - uses: actions/checkout@v6
      - name: Run audit
        uses: soltiHQ/actions/taskfile@main
        with: { command: ci/audit }
